import {FlatListItem} from 'roqay-react-native-common-components';

interface Banner extends FlatListItem {
  id?: number;
  image?: string;
  category?: null;
  product?: null;
}

export default Banner;
